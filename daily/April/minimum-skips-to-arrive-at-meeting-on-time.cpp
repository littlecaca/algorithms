#include <vector>

using namespace std;


class Solution {
public:
    int minSkips(vector<int>& dist, int speed, int hoursBefore) {

        
        // 0-1背包问题
        int m = dist.size(), n = hoursBefore + 1;
        vector<vector<int>> dp(m, vector<int>(n));

        


    }
};